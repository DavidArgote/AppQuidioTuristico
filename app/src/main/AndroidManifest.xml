<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.senasoft.appquindioturistico">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_quindio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".control.control.RegistroActivity"></activity>
        <activity android:name=".control.control.RestaurantesActivity" />
        <activity android:name=".control.control.HotelesActivity" />
        <activity android:name=".control.control.DetalleActivity" />
        <activity android:name=".control.control.SitiosActivity" />
        <activity android:name=".control.control.MainActivity" />
        <activity android:name=".control.control.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>